## 开发规范

- **CLI**: typer 框架
- **包管理**: uv
- **类型注解**: 所有公开函数必须标注
- **注释**: 函数注释简单清晰，优先中文。
- **日志**: log打印。
- **错误处理**: 根据需求情况进行错误处理，cmd相关脚本，要直接raise对外暴露，而不是try catch。
- **配置**: 业务配置用 pyproject.toml，密钥用 .env
- **模型调用**: LLM 的模型调用统一放在 `aiops_bench/llm/` 目录，避免循环依赖

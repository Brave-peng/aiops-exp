Run these commands to test the service:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "demo-service.fullname" . }} 8080:{{ .Values.service.port }}
  curl -sS http://localhost:8080/healthz

